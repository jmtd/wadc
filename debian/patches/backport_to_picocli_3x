Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 wadc (3.1-1) UNRELEASED; urgency=medium
 .
   [ Jonathan Dowland ]
   * Orphan package.
 .
   [ Debian Janitor ]
   * Use secure copyright file specification URI.
   * Bump debhelper from deprecated 9 to 12.
   * Set debhelper-compat version in Build-Depends.
   * Use canonical URL in Vcs-Git.
 .
   [ Jonathan Dowland ]
   * New upstream version
   * Replace glbsp reference in long-description with zdbsp
Author: Jonathan Dowland <jmtd@debian.org>

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: 2021-11-05

--- wadc-3.1.orig/pom.xml
+++ wadc-3.1/pom.xml
@@ -62,7 +62,7 @@
     <dependency>
       <groupId>info.picocli</groupId>
       <artifactId>picocli</artifactId>
-      <version>4.6.1</version>
+      <version>3.9.6</version>
     </dependency>
   </dependencies>
 </project>
--- wadc-3.1.orig/src/main/java/org/redmars/wadc/WadCCLI.java
+++ wadc-3.1/src/main/java/org/redmars/wadc/WadCCLI.java
@@ -17,6 +17,8 @@ import picocli.CommandLine;
 import picocli.CommandLine.Command;
 import picocli.CommandLine.Option;
 import picocli.CommandLine.Parameters;
+import picocli.CommandLine.ParseResult;
+import picocli.CommandLine.ParameterException;
 
 /*
  * an initial, very hacky CLI for WadC
@@ -44,7 +46,22 @@ public class WadCCLI implements WadCMain
     public static void main(String [] args)
     {
         WadCCLI cli = new WadCCLI();
-        System.exit(new CommandLine(cli).execute(args));
+        try
+        {
+            ParseResult pr = new CommandLine(cli).parseArgs(args);
+            if (!CommandLine.printHelpIfRequested(pr))
+            {
+                System.exit(cli.call());
+            }
+        }
+        catch (ParameterException ex)
+        {
+            System.err.println(ex.getMessage());
+            ex.getCommandLine().usage(System.err);
+            System.exit(1);
+        }
+
+        System.exit(cli.call());
     }
 
     // XXX: copied verbatim from MainFrame. should be a static interface method?
@@ -75,7 +92,7 @@ public class WadCCLI implements WadCMain
 
     /* do the magic */
     @Override
-    public Integer call() throws Exception
+    public Integer call()
     {
         readSource(srcFile);
         System.err.println("debug seed: " + initSeed);
