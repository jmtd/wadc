#"standard.h"

/*
 * case of overlapping lines, opposite directions
 */

main {
  quad(right(64))
  rightsector(0, 128, 160)
  thing
 
  movestep(0,16)
  rotleft quad(right(48))
  rightsector(0, 128, 160)
}
